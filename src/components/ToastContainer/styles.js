import styled from 'styled-components';

export const Container = styled.div`
  position: fixed;
  right: 0;
  top: 0;
  margin: 30px;
  /*tudo que está fora do container fica invisível*/
  overflow: hidden;
`;
